import React from 'react';

export const TextField = ({ label, handleChange, value }) => {
  const {} = this.props;
  return (
    <TextField
      id="outlined-basic"
      label={label}
      variant="outlined"
      onChange={handleChange}
      name={label}
      value={value}
    />
  );
};
